import {addUser, removeUser, updateUser} from './actions';
import {archiveUser, unArchiveUser} from './archives';

export default {
  addUser,
  removeUser,
  updateUser,
  archiveUser,
  unArchiveUser
}